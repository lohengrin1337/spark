/**
 * @module utils
 * Shared constants and small helpers used across the modules.
 */

export const TRAIL_MIN_DIST = 1;
export const APPLY_THROTTLE = 1000; //(1000 ms, to be finetuned, no real use as of yet)